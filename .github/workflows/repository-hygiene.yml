name: Repository hygiene

on:
  push:
  pull_request:

jobs:
  hygiene:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run public-repository preflight
        shell: powershell
        run: .\scripts\preflight.ps1
