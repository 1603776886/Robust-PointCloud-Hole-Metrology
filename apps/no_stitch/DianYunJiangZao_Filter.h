/*
================================================================================
文件：DianYunJiangZao_Filter.h
模块：点云降噪接口

【主要职责】
声明项目内点云降噪/离群点处理入口。

【主要调用关系】
主窗口预处理按钮和组合预处理流程调用。

【线程与状态】
纯点云处理，不保存 GUI 状态。

【维护边界】
1. 本文件属于最终稳定结构：日常维护优先整理职责、命名、注释和无语义变化的性能细节，不随意改动已经验证的 Hole 数值判定。
2. Hole 识别阈值、候选排序、ROI、拟合公式、浮点表达式和拼接搜索参数若确需修改，必须单独做生产点云回归，不能夹在结构整理中一起改。
3. 自定义命名遵循“Hole + 拼音 + 基础英文”；Qt/PCL/VTK/Eigen 等第三方官方类型、函数和 API 保持官方名称。
4. 函数注释重点说明“作用、主要调用位置、输入输出/单位、维护风险”；禁止保留只针对历史版本、与当前实现不一致的临时注释。
================================================================================
*/
#pragma once

/*
模块职责：
提供 GUI 当前使用的统计离群点去除，并可按明确参数追加体素下采样。

主要调用位置：
ZhuChuangKou_Window.cpp 的点云预处理按钮。最终 GUI 只保留这一条实际使用的降噪入口。

维护说明：
meanK 是每个点统计近邻的数量：增大后统计更稳定，但计算量增加，也可能削弱很小的局部结构。
GUI 使用固定 K=50 的统计离群点降噪口径，使不同点云规模和扫描姿态采用一致的邻域统计标准。
不同点云规模统一使用同一统计邻域，使预处理口径稳定，并避免邻域大小变化在孔壁上引入额外尺寸偏差。
stddevMult 是标准差阈值倍数：减小会更激进地删除离群点，增大会保留更多原始点。
voxelLeaf 是体素边长，单位与点云坐标一致（当前工程按毫米）；小于等于 0 时不做体素下采样。
*/

#include "DianYunLeixing_Types.h"

#include <cstddef>

CloudPtr tongJiLiQun(
    CloudPtr cloud,
    int meanK = 50,
    double stddevMult = 1.0,
    float voxelLeaf = 0.0f);
